name: Deploy Prod
on:
  workflow_dispatch:
jobs:
  deploy-prod:
    name: Deploy
    runs-on: ubuntu-latest
    environment: development

    steps:
    - name: Print Tag/Branch Name
      run: |
       echo "Triggered tag/branch name: ${{ github.ref }}"
